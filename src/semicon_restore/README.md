# Package Boundary

Reusable dataset, model, loss, metric, checkpoint, runtime, and inference code belongs in this package. CLI scripts under `scripts/` should remain thin argument-parsing entry points.
